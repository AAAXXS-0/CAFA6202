# 长图历史算法归档

本目录只保存已经退出正式流程的历史实现，方便复现实验和比较结果。

```text
连续标题层级_v0.py     # 连续 Title 中首个视为 H2、后续视为 H3 的早期规则
语义标题分析_v1.py     # 模型、墨迹与旧规则加权的第一版；存在相关证据重复计权
```

正式 `semantic` 流程使用上一级的 `步骤004_语义标题分析.py`，不得从这里导入算法。`LocalLongRecognizer` 为兼容旧本地 OCR 输出，仍可显式使用 `连续标题层级_v0.py`；它不参与默认 FinixDoc-VL 预处理。
