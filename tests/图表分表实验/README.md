# 横向分表V1实验

这个目录只测试“一张图里有几张上下排列的表”，不接入正式图表流水线。

- 固定使用原图20%分析图。
- 灰度阈值为225。
- 墨迹只向左右晕染，不向上下扩张。
- 只允许水平分割线，不存在左右分表。
- 已知答案只用于运行结束后的断言，不参与算法判断。
- 输出完整九步中间图和全部白缝JSON，供人工检查。

运行：

```bash
/home/zero/miniconda3/envs/AFAC2026/bin/python tests/图表分表实验/001_横向分表V1.py
```

默认输出：

```text
work/验证/横向分表V1/
```
